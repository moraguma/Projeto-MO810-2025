# Projeto de MO810

Estamos ignorando todas as pastas chamadas `data/` pelo `.gitignore`. É recomendado guardar todos os dados em pastas com este nome para evitar que grandes volumes de dados entrem no repositório remoto